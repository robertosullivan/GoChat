<!--   <div class="field">
    <%= text_field :email, placeholder: 'email' %>
  </div>
  <div class="field">
    <%= password_field :password, placeholder: 'password' %>
  </div>
  <div class="field">
    <%= password_field :password_confirmation, placeholder: 'password' %>
  </div> -->

    <%= bootstrap_form_for @user do |c| %>
  <h3>Edit User</h3>
    <%= c.text_field :email %>
    <%= c.text_field :password %>
    <%= c.text_area :password_confirmation %>
    <%= c.submit ("Submit") %>
  <% end %>

      <% if current_user %>
      Logged in as: <%= current_user.email %><br>
     <%= link_to "Log Out", log_out_path %>
    <% else %>
     <%= link_to "Log In", log_in_path %>
    <% end %>


